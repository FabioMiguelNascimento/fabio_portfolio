export type ExperienceItem = {
  role: string;
  company: string;
  period: string;
  responsibilities: string[];
  logo?: string;
  extension: string;
};

export type EducationItem = {
  course: string;
  institution: string;
  period: string;
};
